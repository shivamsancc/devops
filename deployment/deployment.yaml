apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-deployment
  namespace: my-namespace
spec:
  replicas: 50
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: my-app
          image: superbotdevops/my-app:ededc7a
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: dockerhub-secret  # Name of the Docker Hub secret
